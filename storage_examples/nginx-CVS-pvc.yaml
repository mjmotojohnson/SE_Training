apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-app-volume
spec:
  accessModes:
    - ReadWriteMany
  resources:
      requests:
        storage: 100Gi
  storageClassName: cvs-standard
